<!--
 * @Author: xuwenjie
 * @Date: 2020-10-19 12:05:33
 * @LastEditors: xuwenjie
 * @LastEditTime: 2020-10-23 16:14:45
 * @Description:  
 * @FilePath: /story-book-demo/stories/Button.stories.mdx
-->

<!---Button.stories.mdx --->
import { useState } from 'react';
import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs/blocks';
import { useArgTypes } from '@storybook/api';

import { Button } from './Button';

<Meta title="MDX/Button" component={Button} argTypes={{
    backgroundColor: { control: 'color' },
  }}/>


<!--- This is your Story template function, shown here in React -->

export const Template = (args) => <Button {...args } />

```js

<Meta title="MDX/Button" component={Button} argTypes={{
    backgroundColor: { control: 'color' },
  }}/>


```



# Button  

>This is a multiple types component.

###  Props

<ArgsTable of={Button}/>

We can drop it in a `Canvas` to get a code snippet:


  <Story name='component1'>
    <Button label="label" primary={true} onClick={() => alert('sdfdf')} />
  </Story>

this is other type

<Canvas>
  <Button label="label" primary={true} onClick={() => alert('sdfdf')} />
</Canvas>

  <Story name="sdfsfs" args={{label:"xxxx"}}>
    {Template.bind({})}
  </Story>




<Canvas>
  <Story name='component2'>
     {
       () => {
          const [counter, setCounter] = useState(0);
          const label = `Testing: ${counter}`;
          return (<Button primary={true} onClick={() => setCounter(counter + 1)} label={label}></Button>);
        }
    }
  </Story>
</Canvas>


We can even preview multiple Stories in a block. This
gets rendered as a group, but defines individual stories
with unique URLs which is great for review and testing.
<!-- 
<Canvas>
  <Story name="warning" args={{
      label: 'Warning',
  }}>
    {Template.bind({})}
  </Story>
  <Story name="neutral" args={{
      label: 'Neutral',
  }}>
    {Template.bind({})}
  </Story>
  <Story name="error" args={{
      label: 'Error',
  }}>
    {Template.bind({})}
  </Story>
  <Story name="with icon" args={{
    label:'sdffds',
  )}}>
    {Template.bind({})}
  </Story>
</Story>
</Canvas> -->